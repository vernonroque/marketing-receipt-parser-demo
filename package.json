{
  "name": "receipt-parser-demo",
  "version": "1.0.0",
  "description": "Marketing demo for Receipt Parser API — hosted on Netlify",
  "main": "index.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "busboy": "^1.6.0",
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
